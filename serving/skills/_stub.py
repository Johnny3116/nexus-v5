"""Phase 5 — skill execution engine.

This folder will hold hot-reloadable skill plugins (one file = one skill),
the skill manager (plugin loader), agentic loop, and execution context.

Deferred until orchestration/autonomy/ lands (reflex engine, scheduler,
heartbeat). Skills are the actions those systems trigger.
"""
