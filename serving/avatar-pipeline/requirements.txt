# Avatar pipeline (Riko bridge) — smoke-test dependencies (slim set).
# Python 3.10+. Pinned per CLAUDE.md §2.
# Upstream Riko requirements kept in `requirements.upstream.txt` for
# reference if you reinstate the SoVITS/Gradio/embedding stack.

fastapi[standard]==0.115.2
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-dotenv==1.0.1
PyYAML==6.0.2
openai==1.54.0
httpx==0.27.2

# TTS — Edge-TTS for smoke test (swap to GPT-SoVITS later).
edge-tts==6.1.17
soundfile==0.12.1
sounddevice==0.5.1

# ASR — optional (only needed if testing voice input).
faster-whisper==1.0.3
groq==0.11.0
