{
  "_comment": "MCP server registry. Each key is the server name used in API calls.",
  "_comment2": "Servers are started on first use and kept alive for the session.",

  "filesystem": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "C:\\Users\\Johnn"],
    "description": "Local filesystem access via official MCP server"
  },

  "github": {
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": ""
    },
    "description": "GitHub API — set GITHUB_PERSONAL_ACCESS_TOKEN in env"
  }
}
